import { Routes } from "@angular/router";
import { SessionLayout } from "./components/session-layout/session-layout";

export const CORE_ROUTES: Routes = [
    {
        path: '',
        component: SessionLayout
    }
]